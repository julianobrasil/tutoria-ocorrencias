import { ForceSelectionDirective } from './force-selection.directive';

describe('ForceSelectionDirective', () => {
  // it('should create an instance', () => {
  //   const directive = new ForceSelectionDirective();
  //   expect(directive).toBeTruthy();
  // });
});
